import { FilterRidePipe } from './filter-ride.pipe';

describe('FilterRidePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRidePipe();
    expect(pipe).toBeTruthy();
  });
});
